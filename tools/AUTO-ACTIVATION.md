# التفعيل التلقائي للرخص عند التثبيت

هذا الدليل يشرح كيفية استخدام ميزة التفعيل التلقائي للرخص في Quantiv، والتي تسمح بتفعيل الرخصة تلقائياً عند التثبيت دون الحاجة لتدخل العميل.

## نظرة عامة

تم تطوير نظام التفعيل التلقائي ليحل مشكلة الحاجة لإرسال روابط تفعيل منفصلة للعملاء. الآن يمكن دمج بيانات الرخصة مباشرة في عملية التثبيت.

## كيف يعمل النظام

1. **المثبت يقبل معاملات سطر الأوامر**: `--license-key` و `--license-email`
2. **إنشاء ملف تفعيل مؤقت**: المثبت ينشئ `license-activation.tmp` مع بيانات الرخصة
3. **التفعيل التلقائي عند بدء التطبيق**: التطبيق يقرأ الملف المؤقت ويفعل الرخصة تلقائياً
4. **تنظيف الملف المؤقت**: يتم حذف الملف بعد التفعيل الناجح

## طرق الاستخدام

### الطريقة 1: استخدام أداة توليد الروابط

```bash
# توليد روابط وتعليمات للعميل
node tools/generate-download-link.js --key=ABCD-EFGH-IJKL-MNOP --email=customer@example.com

# مع تحديد إصدار معين
node tools/generate-download-link.js --key=ABCD-EFGH-IJKL-MNOP --email=customer@example.com --version=1.0.5
```

### الطريقة 2: التشغيل المباشر

```cmd
# تحميل المثبت ثم تشغيله مع المعاملات
Quantiv-Setup-1.0.5.exe --license-key="ABCD-EFGH-IJKL-MNOP" --license-email="customer@example.com"
```

### الطريقة 3: PowerShell Script

```powershell
# تحميل وتثبيت وتفعيل في خطوة واحدة
$url = "https://github.com/Khalid1700/Quantiv/releases/download/v1.0.5/Quantiv-Setup-1.0.5.exe"
$output = "$env:TEMP\Quantiv-Setup-1.0.5.exe"
Invoke-WebRequest -Uri $url -OutFile $output
Start-Process -FilePath $output -ArgumentList '--license-key="ABCD-EFGH-IJKL-MNOP","--license-email="customer@example.com"' -Wait
Remove-Item $output -Force
```

### الطريقة 4: Batch File للعملاء

إنشاء ملف `install-quantiv.bat`:

```batch
@echo off
echo Downloading Quantiv...
powershell -Command "Invoke-WebRequest -Uri 'https://github.com/Khalid1700/Quantiv/releases/download/v1.0.5/Quantiv-Setup-1.0.5.exe' -OutFile '%TEMP%\Quantiv-Setup-1.0.5.exe'"
echo Installing with license activation...
"%TEMP%\Quantiv-Setup-1.0.5.exe" --license-key="ABCD-EFGH-IJKL-MNOP" --license-email="customer@example.com"
del "%TEMP%\Quantiv-Setup-1.0.5.exe"
echo Installation complete!
pause
```

## سير العمل للبائع

### 1. إنشاء رخصة للعميل
```bash
# استخدم أداة توليد الرخص الموجودة
node tools/license-generator.js --email=customer@example.com --name="Customer Name"
```

### 2. توليد رابط/تعليمات التثبيت
```bash
# استخدم المفتاح المُولد من الخطوة السابقة
node tools/generate-download-link.js --key=GENERATED_KEY --email=customer@example.com
```

### 3. إرسال التعليمات للعميل
أرسل للعميل إحدى الطرق المُولدة من الأداة:
- رابط التحميل + أمر التشغيل
- PowerShell script جاهز للتنفيذ
- ملف Batch للتحميل والتثبيت التلقائي

## المزايا

✅ **تفعيل تلقائي**: لا حاجة لروابط تفعيل منفصلة  
✅ **تجربة مستخدم محسنة**: خطوة واحدة للتثبيت والتفعيل  
✅ **أمان**: الملف المؤقت يُحذف بعد التفعيل  
✅ **مرونة**: عدة طرق للتوزيع حسب احتياجات العميل  
✅ **توافق**: يعمل مع نظام الرخص الحالي  

## ملاحظات تقنية

- **الملف المؤقت**: `license-activation.tmp` يُنشأ في مجلد التثبيت
- **الأمان**: الملف مخفي ويُحذف بعد التفعيل الناجح
- **التوافق**: يعمل مع Windows NSIS installer
- **الاحتياطي**: إذا فشل التفعيل التلقائي، يظهر حوار التفعيل العادي

## استكشاف الأخطاء

### المثبت لا يقبل المعاملات
- تأكد من استخدام إصدار v1.0.6 أو أحدث
- تأكد من تشغيل المثبت من Command Prompt أو PowerShell

### التفعيل لا يحدث تلقائياً
- تحقق من صحة تنسيق مفتاح الرخصة
- تأكد من صحة عنوان البريد الإلكتروني
- راجع console logs في التطبيق للأخطاء

### ملف التفعيل المؤقت لا يُحذف
- هذا طبيعي إذا فشل التفعيل
- يمكن حذفه يدوياً من مجلد التثبيت
- سيُحذف تلقائياً عند التفعيل الناجح في المحاولة التالية

## الملفات المُعدلة

- `package.json`: إضافة إعدادات NSIS
- `tools/installer-script.nsh`: معالجة معاملات سطر الأوامر
- `main.js`: قراءة ملف التفعيل المؤقت
- `tools/generate-download-link.js`: أداة توليد الروابط

## مثال كامل

```bash
# 1. توليد رخصة
node tools/license-generator.js --email=john@company.com --name="John Doe"
# Output: License Key: ABCD-EFGH-IJKL-MNOP

# 2. توليد رابط التثبيت
node tools/generate-download-link.js --key=ABCD-EFGH-IJKL-MNOP --email=john@company.com

# 3. إرسال التعليمات للعميل (مثال PowerShell):
# أرسل للعميل هذا الأمر ليشغله في PowerShell:
$url = "https://github.com/Khalid1700/Quantiv/releases/download/v1.0.6/Quantiv-Setup-1.0.6.exe"
$output = "$env:TEMP\Quantiv-Setup-1.0.6.exe"
Invoke-WebRequest -Uri $url -OutFile $output
Start-Process -FilePath $output -ArgumentList '--license-key="ABCD-EFGH-IJKL-MNOP","--license-email="john@company.com"' -Wait
Remove-Item $output -Force
```

العميل يشغل الأمر، والتطبيق يُثبت ويُفعل تلقائياً! 🎉